---
import {brushData} from '../../assets/brushData.js'
const {id, title, path, positioning} = Astro.props
---
<svg class=`absolute ${positioning}` viewBox="0 0 300 300" xmlns="http://www.w3.org/2000/svg">
  <path
    id={id}
    fill="none"
    stroke="none"
    d={path} />
  <text x="0" font-size="22">
    <textPath href=`#${id}`>{title}</textPath>
  </text>
</svg>
