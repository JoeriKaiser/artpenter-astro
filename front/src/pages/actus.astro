---
import Description from "@components/decorative/description.astro";
import Layout from "@layouts/Layout.astro";
import { COLORS } from "@utils/constants";
import ListView from "../views/ListView/ListView";
import PageHeader from "views/pageHeader.astro";

const response = await fetch(`https://${import.meta.env.PUBLIC_BACKEND_URL}/api/actu?populate=*&sort=rank:asc`,
{
    method: "GET",
    headers: {
      Authorization: `Bearer ${import.meta.env.API_TOKEN}`
    },
  });
const data = await response.json();
---

<Layout title="Actus">
  <PageHeader
    mainBrushColor={COLORS.purpleLight}
    secondaryBrushColor={COLORS.black}
    title="ACTUS"
    subtitle="Actualités du moment"
    id="5"
  />
  <!-- <Description id={5} /> -->
  <!-- {data && (
    <ListView client:load data={data.data} />
  )} -->
</Layout>
